# Review Task

- Read `AGENTS.md`, the task specification, and the fixed review base.
- Use `code-review` for specification and standards review.
- Use `code-review-and-quality` for correctness, security, maintainability, accessibility, and performance.
- Run `ocr delegate preview`, resolve rules with `ocr delegate rule <paths>`, then review each selected diff.
- Report Critical, High, and contextual Medium findings. Do not auto-fix, auto-commit, or auto-merge.