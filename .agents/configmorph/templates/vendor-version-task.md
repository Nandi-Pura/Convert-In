# Vendor Version Task

- Read `AGENTS.md`.
- Use `source-driven-development`, `diagnosing-bugs`, and TDD.
- Use official vendor documentation only; record reference ID and exact OS-version profile.
- Never inherit adjacent-version behavior or map an unknown patch to a supported patch.
- Treat parser and renderer capability independently.
- Regress existing exact versions and release lines.
- Run `code-review`, then OCR delegation review.