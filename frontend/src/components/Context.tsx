import {useState} from 'react';
import {percentages} from '../accounting';
import type {Profile,Result} from '../types';

const label=(value:string)=>value.replaceAll('_',' '),fmt=(n:number)=>n.toLocaleString();
export function SourceImport({max,onLoad}:{max:number;onLoad:(text:string,name:string)=>void}){const[paste,setPaste]=useState(false),[value,setValue]=useState('');return <div className="source-import"><b>No source configuration loaded.</b>{paste?<div className="paste-editor"><textarea aria-label="Configuration" value={value} onChange={e=>setValue(e.target.value)} placeholder="Paste configuration" autoFocus/><small>Maximum UTF-8 size: {fmt(max)} bytes</small><div><button onClick={()=>setPaste(false)}>Cancel</button><button className="primary" disabled={!value.trim()} onClick={()=>{onLoad(value,'Pasted configuration');setPaste(false);setValue('')}}>Use Configuration</button></div></div>:<div className="import-actions"><label className="file">Open Configuration<input type="file" accept=".cfg,.conf,.txt,.xml" onChange={async e=>{const f=e.target.files?.[0];if(f)onLoad(await f.text(),f.name)}}/></label><button onClick={()=>setPaste(true)}>Paste Configuration</button></div>}</div>}
function Selector({title,profiles,profile,version,onProfile,onVersion,detected}:{title:string;profiles:Profile[];profile:string;version:string;onProfile:(v:string)=>void;onVersion:(v:string)=>void;detected?:string}){const selected=profiles.find(p=>p.id===profile),versions=[...new Set([detected,...(selected?.version_profiles?.map(v=>v.exact_version)||selected?.supported_versions||[])].filter(Boolean))] as string[];return <fieldset className="selector"><legend>{title}</legend><label>Domain<select value={selected?.domain||''} disabled><option>{selected?.domain||'Select'}</option></select></label><label>Vendor<select value={profile} onChange={e=>onProfile(e.target.value)}>{profiles.map(p=><option value={p.id} key={p.id}>{label(p.vendor)}</option>)}</select></label><label>Platform<select value={profile} onChange={e=>onProfile(e.target.value)}>{profiles.map(p=><option value={p.id} key={p.id}>{label(p.platform)}</option>)}</select></label><label>Version<select value={version} onChange={e=>onVersion(e.target.value)}>{versions.map(v=><option key={v}>{v}</option>)}</select></label></fieldset>}
function Summary({result}:{result?:Result}){if(!result)return <section className="conversion empty-summary" data-testid="conversion-summary"><b>No analysis available</b><span>0 lines analyzed</span></section>;const a=result.line_accounting,p=percentages(a),items=[['Converted / Ready',a.converted_lines],['Review Required',a.review_lines],['Not Supported',a.unsupported_lines],['Unchanged / Other',a.unchanged_lines]];return <section className="conversion" data-testid="conversion-summary"><div className="ring" style={{background:`conic-gradient(#22965a 0 ${p[0]}%,#dda317 ${p[0]}% ${p[0]+p[1]}%,#c73b3b ${p[0]+p[1]}% ${p[0]+p[1]+p[2]}%,#8190a3 0)`}}><b>{fmt(a.total_analyzed_lines)}</b><small>lines analyzed</small></div><div>{items.map((item,i)=><p key={item[0]}><i className={`dot d${i}`}/><b>{p[i]}%</b><span>{item[0]}</span><small>{fmt(item[1] as number)} lines</small></p>)}</div></section>}
export function MigrationContext(props:{profiles:Profile[];max:number;text:string;name:string;sourceProfile:string;sourceVersion:string;targetProfile:string;targetVersion:string;result?:Result;running:boolean;stale:boolean;onLoad:(t:string,n:string)=>void;onRun:()=>void;onRemove:()=>void;onSourceProfile:(v:string)=>void;onSourceVersion:(v:string)=>void;onTargetProfile:(v:string)=>void;onTargetVersion:(v:string)=>void}){const[editSource,setEditSource]=useState(false),[editTarget,setEditTarget]=useState(false),source=props.profiles.find(p=>p.id===props.sourceProfile),target=props.profiles.find(p=>p.id===props.targetProfile),domain=source?.domain,sourceProfiles=props.profiles.filter(p=>p.domain===domain&&p.source_vendor),targetProfiles=props.profiles.filter(p=>p.domain===domain&&p.target_capability!=='TARGET_NOT_IMPLEMENTED');return <section className="context"><article data-testid="source-context"><small>Source Configuration</small>{!props.text?<SourceImport max={props.max} onLoad={props.onLoad}/>:editSource?<Selector title="Edit source" profiles={sourceProfiles} profile={props.sourceProfile} version={props.sourceVersion} detected={props.sourceVersion} onProfile={props.onSourceProfile} onVersion={props.onSourceVersion}/>:<><b>{source?.vendor&&label(source.vendor)} {source?.platform&&label(source.platform)}</b><span>{source?.os_family||label(source?.platform||'')} {props.sourceVersion}</span><em>{props.name} · {(props.text.split('\n').length).toLocaleString()} lines</em><div className="context-actions"><button onClick={()=>setEditSource(true)}>Edit Source</button><button onClick={props.onRemove}>Remove</button></div></>}</article><div className="flow" aria-hidden="true">→</div><article data-testid="target-context"><small>Target Configuration</small>{!props.text?<><b>No target selected.</b><span>Load a source configuration first.</span></>:editTarget?<Selector title="Change target" profiles={targetProfiles} profile={props.targetProfile} version={props.targetVersion} onProfile={props.onTargetProfile} onVersion={props.onTargetVersion}/>:<><b>{target?`${label(target.vendor)} ${label(target.platform)}`:'Select target'}</b><span>{target?.os_family||''} {props.targetVersion}</span><em>Exact release: {props.targetVersion||'Not selected'}</em><button onClick={()=>setEditTarget(true)}>Change Target</button></>}</article><article className="actions"><small>Actions</small><button className="primary" disabled={props.running||!props.text||!props.sourceProfile||!props.targetProfile} onClick={props.onRun}>{props.running?'Processing...':!target||target.target_capability.includes('RENDERER')?'Analyze / Convert':'Analyze'}</button><button disabled={!props.text}>Conversion Options</button>{props.stale&&<strong className="stale">Migration context changed. Previous analysis is stale. Run Analyze / Convert again.</strong>}</article><Summary result={props.stale?undefined:props.result}/></section>}