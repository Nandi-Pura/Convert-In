interface Props { projectId?:string; saved?:boolean; onOpen:()=>void; onImport:()=>void }
export default function Header({projectId,saved=Boolean(projectId),onOpen,onImport}:Props){return <header className="figma-header"><div className="figma-brand"><b>ConfigMorph</b><span>Network Configuration Migration Workbench<small>Visualize. Analyze. Migrate.</small></span></div><div className="figma-project"><span>{projectId?`Project ${projectId.slice(0,8)}`:'Untitled Project'}</span><strong className={saved?'saved':'modified'}>● {saved?'Saved':'Modified'}</strong><button onClick={onOpen}>▱ Open</button>{projectId?<a href={`/api/projects/${projectId}/export`}>⇧ Export Project</a>:<button disabled>⇧ Export Project</button>}<button onClick={onImport}>⇩ Import Project</button><em>● Local Only</em></div></header>}