import type{Evidence,Manifest,MigrationPlan,Operation,Result}from'./types';
async function json<T>(url:string,init?:RequestInit):Promise<T>{const r=await fetch(url,init);const data=await r.json();if(!r.ok)throw new Error(data.detail||`HTTP ${r.status}`);return data}
export const api={detect:(source_text:string,signal?:AbortSignal)=>json<{vendor:string;version?:string;domain:string}>('/api/convert/detect',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({source_text}),signal}),start:(body:object)=>json<{operation_id:string}>('/api/workbench/operations',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)}),operation:(id:string,signal?:AbortSignal)=>json<Operation>(`/api/operations/${id}`,{signal}),project:(id:string,signal?:AbortSignal)=>json<{manifest:Manifest;source_text:string;result:Result}>(`/api/projects/${id}`,{signal}),importProject:(file:File)=>{const b=new FormData();b.set('project',file);return json<Manifest>('/api/projects/import',{method:'POST',body:b})},plan:(id:string)=>json<MigrationPlan>(`/api/projects/${id}/migration/plan`,{method:'POST'}),evidence:(id:string)=>json<Evidence>(`/api/projects/${id}/migration/evidence-pack`,{method:'POST'})};