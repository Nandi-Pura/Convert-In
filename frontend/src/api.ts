import type{Manifest,Operation,Result}from'./types';
async function json<T>(url:string,init?:RequestInit):Promise<T>{const r=await fetch(url,init);const data=await r.json();if(!r.ok)throw new Error(data.detail||`HTTP ${r.status}`);return data}
export const api={detect:(source_text:string)=>json<{vendor:string;version?:string;domain:string}>('/api/convert/detect',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({source_text})}),start:(body:object)=>json<{operation_id:string}>('/api/workbench/operations',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)}),operation:(id:string)=>json<Operation>(`/api/operations/${id}`),project:(id:string)=>json<{manifest:Manifest;source_text:string;result:Result}>(`/api/projects/${id}`),importProject:(file:File)=>{const b=new FormData();b.set('project',file);return json<Manifest>('/api/projects/import',{method:'POST',body:b})},evidence:(id:string)=>json<{fingerprint:string;artifacts:{status:string}[]}>(`/api/projects/${id}/migration/evidence-pack`,{method:'POST'})};